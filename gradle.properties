org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
# Configure a custom JDK location locally via GRADLE_USER_HOME/gradle.properties if needed.

# Optimization for CI/CD
# Disable configuration cache for stability (can cause issues with Android plugin)
org.gradle.configuration-cache=false
# Use parallel execution for faster builds (but not during tests)
org.gradle.parallel=true
# Enable caching for faster subsequent builds
org.gradle.caching=true
